import '../Help.scss';

export default function HelpAdd() {
    return (
        <div>
            <h2>Adding Tasks</h2>
            <p>In order to add tasks, fill out the form on the add task page and hit submit.</p>
        </div>
    );
}