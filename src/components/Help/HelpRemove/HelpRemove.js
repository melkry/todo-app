import '../Help.scss';

export default function HelpRemove() {
    return (
        <div>
            <h2>Removing Tasks</h2>
            <p>In order to remove tasks, click the "Remove Task" button below a task.</p>
        </div>
    );
}