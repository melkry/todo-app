import '../Help.scss';

export default function HelpChange() {
    return (
        <div>
            <h2>Changing Task Status</h2>
            <p>In order to change a task's status, use the toggle button under a task labelled "Change Status". If a task is labelled complete, this will label it incomplete, and vice versa.</p>
        </div>
    );
}